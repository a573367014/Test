{
  "name": "@facai/utils",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "vue-tsc --noEmit && vite build",
    "watch": "chokidar 'src/*' -c 'yarn build' --polling"
  },
  "dependencies": {
  },
  "devDependencies": {
    "chokidar-cli": "3.0.0",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7"
  }
}